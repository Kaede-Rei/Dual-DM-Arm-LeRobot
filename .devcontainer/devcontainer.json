{
    "name": "LeRobot",
    "build": {
        "dockerfile": "Dockerfile"
    },
    "containerEnv": {
        // Docker Engine 下用宿主机真实 IP 或 172.17.0.1（docker0 网关）
        // 运行 ip addr show docker0 查看，通常是 172.17.0.1
        "http_proxy": "http://172.17.0.1:7897",
        "https_proxy": "http://172.17.0.1:7897",
        "all_proxy": "socks5://172.17.0.1:7897",
        "no_proxy": "localhost,127.0.0.1"
    },
    "shutdownAction": "stopContainer",
    "customizations": {
        "vscode": {
            "extensions": [
                "ms-python.python",
                "ms-python.vscode-pylance",
                "mhutchie.git-graph",
                "ms-python.black-formatter",
                "wangguixuan.var-translate-en",
                "uctakeoff.vscode-counter"
            ]
        }
    },
    "runArgs": [
        "--privileged",
        "--group-add=video",
        "--group-add=dialout",
        "--network=host"
    ],
    "mounts": [
        "source=/dev,target=/dev,type=bind,consistency=cached"
    ],
    // 用 postCreateCommand 安装项目依赖
    "postCreateCommand": "pip install -e '.[all]' 2>/dev/null || pip install -e . || true"
}